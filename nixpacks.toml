[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = [
    "cd backend && mvn -Pprod -DskipTests clean package"
]

[start]
cmd = "cd backend && java -jar target/backend-0.0.1-SNAPSHOT.jar"
